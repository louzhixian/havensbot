# ArkCore 架构设计文档

> 最后更新：2026-01-27

## 1. 项目概述

ArkCore 是一个 **Discord RSS 聚合 Bot**，核心能力是：
- 订阅 RSS 源，定时抓取并去重
- 生成每日 Digest（可选 LLM 摘要）
- 提供多种交互式功能（收藏、深度阅读、写作助手、日记等）

## 2. 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Discord Guild                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ Source Channels │  │ Digest Forum │  │ Editorial Ch │  │ Diary Forum  │    │
│  │ (RSS 订阅源)    │  │ (摘要输出)   │  │ (写作/翻译)  │  │ (日记)       │    │
│  └───────┬──────┘  └───────▲──────┘  └───────▲──────┘  └───────▲──────┘    │
│          │                 │                 │                 │           │
└──────────┼─────────────────┼─────────────────┼─────────────────┼───────────┘
           │                 │                 │                 │
           ▼                 │                 │                 │
┌──────────────────────────────────────────────────────────────────────────────┐
│                            ArkCore Bot (Node.js)                             │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                        Entry Layer (index.ts)                        │    │
│  │  • Discord.js Client 初始化                                          │    │
│  │  • 注册所有 Handler（commands, reactions, messages）                  │    │
│  │  • 启动 Scheduler                                                    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                         │
│         ┌──────────────────────────┼──────────────────────────┐             │
│         ▼                          ▼                          ▼             │
│  ┌─────────────┐          ┌─────────────┐          ┌─────────────┐         │
│  │  Commands   │          │  Scheduler  │          │  Reactions  │         │
│  │ (Slash Cmd) │          │ (node-cron) │          │  Handlers   │         │
│  └──────┬──────┘          └──────┬──────┘          └──────┬──────┘         │
│         │                        │                        │                 │
│         └────────────────────────┼────────────────────────┘                 │
│                                  ▼                                          │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                         Core Modules                                  │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐             │   │
│  │  │   RSS    │  │  Digest  │  │ Messaging│  │   LLM    │             │   │
│  │  │ (抓取)   │  │ (摘要)   │  │ (输出)   │  │ (Client) │             │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘             │   │
│  └───────┼─────────────┼─────────────┼─────────────┼────────────────────┘   │
│          │             │             │             │                        │
│  ┌───────┼─────────────┼─────────────┼─────────────┼────────────────────┐   │
│  │       │         Feature Modules   │             │                    │   │
│  │  ┌────▼────┐  ┌─────────┐  ┌─────▼────┐  ┌────▼─────┐  ┌─────────┐ │   │
│  │  │Favorites│  │ Deeper  │  │Editorial │  │  Diary   │  │Readings │ │   │
│  │  │ ❤️ 收藏 │  │ 👀 深读 │  │ 写作/翻译│  │ 📔 日记  │  │ 🔖 书签 │ │   │
│  │  └─────────┘  └─────────┘  └──────────┘  └──────────┘  └─────────┘ │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                  │                                          │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                      Infrastructure Layer                             │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐             │   │
│  │  │  Prisma  │  │  Logger  │  │ Metrics  │  │ Archival │             │   │
│  │  │   (DB)   │  │  (Pino)  │  │ & Alerts │  │ (清理)   │             │   │
│  │  └────┬─────┘  └──────────┘  └──────────┘  └──────────┘             │   │
│  └───────┼──────────────────────────────────────────────────────────────┘   │
│          │                                                                  │
└──────────┼──────────────────────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────┐          ┌──────────────────────┐
│     PostgreSQL       │          │    LLM Provider      │
│  (Prisma managed)    │          │  (OpenAI-compatible) │
└──────────────────────┘          └──────────────────────┘
```

## 3. 核心数据流

### 3.1 RSS → Digest 流程

```
┌─────────────┐    定时触发     ┌─────────────┐    存储      ┌─────────────┐
│  RSS Feeds  │ ──────────────▶ │  rss.ts     │ ──────────▶ │   Item      │
│  (外部源)    │   (每 N 分钟)   │  抓取/解析   │             │  (DB 表)    │
└─────────────┘                 └─────────────┘             └──────┬──────┘
                                                                   │
                                                                   ▼
┌─────────────┐    Discord      ┌─────────────┐    聚合      ┌─────────────┐
│  Digest     │ ◀────────────── │ messaging.ts│ ◀────────── │ digest.ts   │
│  Forum/Ch   │   发送消息       │  格式化输出  │             │ 生成摘要    │
└─────────────┘                 └─────────────┘             └─────────────┘
                                                                   │
                                                           ┌───────┴───────┐
                                                           ▼               ▼
                                                    ┌──────────┐    ┌──────────┐
                                                    │ Rule-based│    │ LLM 摘要 │
                                                    │ (fallback)│    │ (可选)   │
                                                    └──────────┘    └──────────┘
```

### 3.2 Reaction 触发流程

```
用户反应 emoji
      │
      ▼
┌─────────────┐
│ Reaction    │
│ Handler     │
└──────┬──────┘
       │
       ├── ❤️ ──▶ favorites.ts ──▶ 转发到收藏频道
       │
       ├── 👀 ──▶ deeper.ts ──▶ LLM 深度分析 ──▶ 创建讨论帖
       │
       └── 🔖 ──▶ readings.ts ──▶ 创建阅读帖 ──▶ Q&A 交互
```

## 4. 模块职责

### 4.1 入口层

| 文件 | 职责 |
|------|------|
| `index.ts` | 应用入口：初始化 Discord client，注册所有 handler，启动 scheduler |
| `discord.ts` | Discord client 创建和命令注册 |
| `config.ts` | 配置加载（环境变量 → AppConfig） |

### 4.2 命令层

| 文件 | 职责 |
|------|------|
| `commands.ts` | Discord Slash Commands 处理（`/source`, `/fetch`, `/digest`, `/editorial` 等） |

### 4.3 调度层

| 文件 | 职责 |
|------|------|
| `scheduler.ts` | 定时任务管理（node-cron） |

**定时任务清单：**

| 任务 | 默认周期 | 功能 |
|------|----------|------|
| RSS Fetch | 每 10 分钟 | 抓取所有订阅源 |
| Digest | 每天 09:00 | 生成并发送 Digest |
| Alert Check | 每小时 | 检查告警规则 |
| Daily Report | 每天 20:00 | 发送运营日报 |
| Archival | 每周日 02:00 | 清理过期数据 |
| Diary Post | 每天 23:00 | 创建日记帖（如启用） |
| Diary Timeout | 每 5 分钟 | 检查超时日记会话 |

### 4.4 核心模块

| 文件 | 职责 |
|------|------|
| `rss.ts` | RSS 抓取、解析、去重、条件式 ETag/Last-Modified |
| `digest.ts` | 聚合 Item → 生成 Digest（LLM 可选） |
| `messaging.ts` | Discord 消息发送、Thread 模式、Forum 帖子 |
| `llm/client.ts` | 统一 LLM 客户端（retry、metrics、fallback） |

### 4.5 功能模块

| 文件 | 职责 |
|------|------|
| `favorites.ts` | ❤️ 反应转发到收藏频道 |
| `deeper.ts` | 👀 反应触发 LLM 深度分析 |
| `editorial-discussion.ts` | 转发消息 → 创作讨论帖 |
| `editorial-translation.ts` | URL/文本 → 翻译帖 |
| `readings.ts` | 🔖 书签 → 阅读帖 + Q&A |
| `diary/` | 日记功能（定时创建、会话管理、LLM 陪伴） |
| `voice/` | 语音消息 → 文字转录 (Whisper) |

### 4.6 基础设施

| 文件/目录 | 职责 |
|-----------|------|
| `db.ts` | Prisma client 实例 |
| `channel-config.ts` | 频道角色配置管理（DB 驱动） |
| `observability/` | 日志(Pino)、指标记录、告警规则、Discord 通知 |
| `archival/` | 数据归档（软删除 Item、清理 Metrics/Alerts） |
| `utils/` | 通用工具（retry、date、text 处理） |

## 5. 数据模型

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Core Models                                    │
│                                                                             │
│  ┌──────────┐       1:N       ┌──────────┐                                 │
│  │  Source  │ ───────────────▶│   Item   │                                 │
│  │ (RSS 源) │                 │ (条目)   │                                 │
│  └──────────┘                 └──────────┘                                 │
│       │                             │                                       │
│       │ channelId                   │ 字段：                                │
│       │                             │ - title, url, publishedAt             │
│       │                             │ - contentSnippet, contentHash         │
│       │                             │ - writingSuggestions (JSON)           │
│       │                             │ - deepDive (深度分析结果)              │
│       │                             │ - archivedAt (软删除)                  │
│       │                             │                                       │
│  ┌──────────┐                 ┌──────────┐                                 │
│  │  Digest  │                 │Editorial │                                 │
│  │ (摘要)   │                 │ Report   │                                 │
│  └──────────┘                 └──────────┘                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           Feature Models                                    │
│                                                                             │
│  ┌──────────────┐            ┌──────────────┐                              │
│  │ DiarySession │            │ChannelConfig │                              │
│  │ (日记会话)   │            │ (频道角色)   │                              │
│  └──────────────┘            └──────────────┘                              │
│        │                            │                                       │
│        │ 字段：                     │ role:                                 │
│        │ - date, threadId          │ - digest_source (订阅源分类)           │
│        │ - messageCount            │ - digest_output (摘要输出)             │
│        │ - endReason, exportPath   │ - deep_dive_output                    │
│        │                           │ - diary, favorites, editorial         │
│        │                           │ - readings                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                        Observability Models                                 │
│                                                                             │
│  ┌───────────────────┐            ┌──────────────┐                         │
│  │ObservabilityMetric│            │ SystemAlert  │                         │
│  │ (指标记录)        │            │ (系统告警)   │                         │
│  └───────────────────┘            └──────────────┘                         │
│         │                               │                                   │
│         │ type:                         │ alertType:                        │
│         │ - llm_call                    │ - llm_failure_rate                │
│         │ - rss_fetch                   │ - llm_cost_high                   │
│         │ - digest_run                  │ - storage_warning                 │
│         │ - editorial_run               │ - rss_failure                     │
│         │ - deeper_run                  │ - digest_failure                  │
│         │ - whisper_transcribe          │                                   │
│         │ - discord_message             │                                   │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 6. 配置体系

配置通过 **环境变量** 加载，`config.ts` 解析为 `AppConfig` 对象。

### 6.1 核心配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DISCORD_BOT_TOKEN` | 必填 | Bot Token |
| `DISCORD_APPLICATION_ID` | 必填 | Application ID |
| `DISCORD_GUILD_ID` | 必填 | Guild ID |
| `DATABASE_URL` | 必填 | PostgreSQL 连接串 |
| `TZ` | `Asia/Tokyo` | 时区 |

### 6.2 RSS & Digest

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `FETCH_INTERVAL_MINUTES` | `10` | RSS 抓取间隔 |
| `DIGEST_CRON` | `0 9 * * *` | Digest 发送时间 |
| `DIGEST_THREAD_MODE` | `true` | Thread 模式发送 |
| `DIGEST_MAX_ITEMS` | `30` | 单次 Digest 最大条目 |
| `NEW_SOURCE_MAX_ITEMS` | `3` | 新源首次抓取上限 |

### 6.3 LLM

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `LLM_PROVIDER` | `none` | `none` / `openai_compat` |
| `LLM_API_KEY` | - | API Key |
| `LLM_BASE_URL` | `https://api.openai.com` | API 端点 |
| `LLM_MODEL` | - | 模型名 |
| `LLM_MAX_TOKENS` | `4000` | 最大输出 token |
| `LLM_TIMEOUT_MS` | `120000` | 请求超时 |

### 6.4 功能开关

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `VOICE_TO_TEXT_ENABLED` | `false` | 语音转文字 |
| `DIARY_ENABLED` | `false` | 日记功能 |
| `ARCHIVE_ENABLED` | `true` | 数据归档 |
| `DAILY_REPORT_ENABLED` | `true` | 每日报告 |

## 7. 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Docker Compose                           │
│                                                             │
│  ┌─────────────────┐       ┌─────────────────┐             │
│  │    arkcore      │       │    postgres     │             │
│  │  (Node.js Bot)  │ ────▶ │  (Database)     │             │
│  └─────────────────┘       └─────────────────┘             │
│           │                                                 │
│           │ (可选)                                          │
│           ▼                                                 │
│  ┌─────────────────┐                                       │
│  │    rsshub       │  ← 已移除，改用官方 RSS               │
│  │  (Feed Adapter) │                                       │
│  └─────────────────┘                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
          │
          │ External
          ▼
┌─────────────────────────────────────────────────────────────┐
│  • Discord API                                              │
│  • LLM Provider (OpenAI / Gemini / 其他兼容)                │
│  • Whisper API (语音转文字)                                 │
│  • RSS Feeds (外部订阅源)                                   │
└─────────────────────────────────────────────────────────────┘
```

## 8. 关键设计决策

详见 `DECISIONS.md`，核心决策摘要：

| 领域 | 决策 |
|------|------|
| **去重** | `contentHash = sha256(url + title + publishedAt)` |
| **新源回填** | 首次抓取限制 3 条，避免历史洪水 |
| **LLM 容错** | 支持 fallback（rule-based）、JSON 修复、部分提取 |
| **Digest 投递** | Thread 模式：概览在频道，详情在 Thread |
| **可观测性** | Pino 日志 + Prisma Metrics + Discord 告警 |
| **数据归档** | 软删除（`archivedAt`），90 天 Metrics 保留 |

## 9. 扩展点

### 9.1 添加新的 Reaction Handler

1. 在 `index.ts` 注册 handler
2. 实现 `register*Handler(client, config)` 函数
3. 在 `channel-config.ts` 添加新 role（如需要）

### 9.2 添加新的 Slash Command

1. 在 `commands.ts` 的 `COMMANDS` 数组添加定义
2. 在 `handleInteraction()` 添加处理逻辑

### 9.3 添加新的定时任务

1. 在 `config.ts` 添加配置项
2. 在 `scheduler.ts` 的 `startSchedulers()` 添加 cron job

### 9.4 添加新的 LLM 功能

1. 在 `prompts/` 添加 prompt 文件
2. 使用 `createLlmClient(config).call()` 或 `.callWithFallback()`
3. 在 `DECISIONS.md` 记录 prompt 变更原因

## 10. 目录结构

```
ArkCore/
├── apps/arkcore/src/
│   ├── index.ts              # 入口
│   ├── config.ts             # 配置
│   ├── commands.ts           # Slash Commands
│   ├── scheduler.ts          # 定时任务
│   ├── discord.ts            # Discord client
│   ├── db.ts                 # Prisma client
│   │
│   ├── rss.ts                # RSS 抓取
│   ├── digest.ts             # Digest 生成
│   ├── messaging.ts          # 消息输出
│   │
│   ├── favorites.ts          # ❤️ 收藏
│   ├── deeper.ts             # 👀 深度阅读
│   ├── readings.ts           # 🔖 书签
│   ├── editorial-*.ts        # 写作助手
│   │
│   ├── channel-config.ts     # 频道配置
│   ├── source-handlers.ts    # Source 处理
│   ├── utils.ts              # 工具函数
│   │
│   ├── llm/                  # LLM 客户端
│   ├── diary/                # 日记模块
│   ├── voice/                # 语音模块
│   ├── readings/             # 阅读模块子组件
│   ├── observability/        # 可观测性
│   ├── archival/             # 归档
│   └── utils/                # 工具类
│
├── prisma/
│   └── schema.prisma         # 数据模型
│
├── prompts/                  # LLM Prompts
│   ├── README.md
│   ├── deeper.article_summary.prompt.md
│   ├── diary.companion.prompt.md
│   ├── editorial.*.prompt.md
│   └── readings.qa.prompt.md
│
├── docs/
│   ├── README.md             # 文档目录
│   ├── ARCHITECTURE.md       # 本文档
│   ├── features.md           # 功能追踪
│   ├── plans/                # 设计文档
│   └── templates/            # 文档模板
│
├── scripts/                  # 运维脚本
├── AGENTS.md                 # Agent 工作流
├── DECISIONS.md              # 决策记录
├── RUNBOOK.md                # 运维手册
├── README.md                 # 项目说明
├── docker-compose.yml        # 部署配置
└── .env.example              # 环境变量模板
```

## 11. 参考文档

- `README.md` - 快速开始
- `DECISIONS.md` - 所有架构决策
- `RUNBOOK.md` - 运维和故障排查
- `docs/features.md` - 功能状态追踪
- `docs/plans/` - 各功能详细设计
- `prompts/README.md` - Prompt 管理规范
